[project]
name = "my_app"
version = "0.1.0"
dependencies = [
    "opencv-python",
    "numpy",
    "pyside6",
    "cmapy",
    "scipy"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"